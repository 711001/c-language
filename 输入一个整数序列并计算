#include <stdio.h>

int main()
{
    int sum=0 ,current;
    do{
        scanf("%d",&current);
        if(current>0){
            sum+=current;
        }

    }while(current>0);
    printf("sum is %d",sum);
    return 0;
}
//  http://c.biancheng.net/cpp/html/2543.html
